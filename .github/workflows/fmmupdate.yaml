name: Generate files manifest (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fmtree:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Generate files.json (no commit)
        run: python3 fmtree.py